import styles from './Loagind.module.css';

function Loading() {
  return (
    <section className={ styles.loadingPage }>
      <h1 className={ styles.loaging }>Loading...</h1>
    </section>
  );
}

export default Loading;
